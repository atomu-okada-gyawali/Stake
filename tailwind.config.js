theme: {
  extend: {
    colors: {
      stake: {
        bg: '#131313',        // Deep background
        card: '#262626',      // Card background
        accent: '#BBF400',    // Signature Lime
        muted: '#C4C9AC',    // Muted text/borders
        logout: '#FFB4AB'     // Log out text color
      }
    },
    fontFamily: {
      poppins: ['Poppins', 'sans-serif'],
    }
  }
}